import React from 'react';
export const metadata = {
  title: "KeenKeeper- Stats",
};

const Stats = () => {
    return (
        <div>
            this is stat
            
        </div>
    );
};

export default Stats;